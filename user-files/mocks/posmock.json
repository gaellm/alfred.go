{
    "name": "postmock",
    "request": {
        "method": "POST",
        "url": "/some/thing/:test.tyty"
    },
    "response": {
        "status": 200,
        "body": "Hello world! {{ alfred.req.test.titi }} and {{ alfred.req.test.toto }} and re {{ alfred.req.test.toto }} but not {{ alfred.req.test.tutu }}",
        "headers": {
            "Content-Type": "text/plain",
            "Test": "{{ alfred.req.test.tyty }}"
        }
    }
}