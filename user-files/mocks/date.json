{
    "name": "datemock",
    "request": {
        "method": "GET",
        "url": "/some/date"
    },
    "response": {
        "status": 200,
        "body": "Hello world! with date {{ alfred.time.now }} two times {{ alfred.time.now @name:'date2' }} or {{ alfred.time.now.utc }}",
        "headers": {
            "Content-Type": "text/plain"
        }
    }
  }